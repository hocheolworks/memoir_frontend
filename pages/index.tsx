import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <main className="h-full w-full flex items-center justify-center">
      <div>HOME</div>
    </main>
  );
};

export default Home;
