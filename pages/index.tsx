import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <main className="flex items-center justify-center w-full h-full">
      <div>HOME</div>
    </main>
  );
};

export default Home;
